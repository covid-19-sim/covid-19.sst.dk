# Enevironment

Use pipenv:

```shell script
pipenv install
pipenv shell
```

# Updating files

```shell script
python update_raw.py
python raw2total.py
git commit -am "Updated with todays numbers"
```
